[default]
locale = "en"
extend-ignore-re = [
    # Test examples with intentional typos for suggestion testing
    "adnlp_backen",
    "ipopt_backen",
]

[files]
extend-exclude = [
    "*.json",
    "*.toml",
    "*.svg",
]
